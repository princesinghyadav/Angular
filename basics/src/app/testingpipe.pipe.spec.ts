import { TestingpipePipe } from './testingpipe.pipe';

describe('TestingpipePipe', () => {
  it('create an instance', () => {
    const pipe = new TestingpipePipe();
    expect(pipe).toBeTruthy();
  });
});
